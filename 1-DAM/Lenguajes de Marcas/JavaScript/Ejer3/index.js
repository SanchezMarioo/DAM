let numero = Number(prompt("Introduce el numero: "));
for (let index = 1; index <= 10; index++) {
  if (!Number.isNaN) {
    document.writeln(
      "<p>" + numero + " * " + index + " = " + numero * index + "</p>"
    );
  } else{
    document.write("<p>Esto no es un numero</p>")
    break;
  }
}
